#
# A sample configuration file that can be used to run gld in "embedded cache" mode
#
service:

  type: cache
  implementation: embedded

  key-size: 10
  value-size: 1024

  load-strategy:
    name: read-then-write-on-miss
    use-different-values: true

load:
  threads: 1

