#
# reference configuration for a load driver instance
#
service:
  # service types: cache, jms, http
  type: cache
  implementation: local
  load-strategy:
    name: read-then-write-on-miss

load:
  threads: 10